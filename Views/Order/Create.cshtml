@using System.Linq
@model ProductManagement.Models.Order


@{
    ViewData["Title"] = "Thêm sản phẩm vào đơn hàng";
}

<script src="https://code.jquery.com/jquery-3.7.0.js"></script>
<script src="https://code.jquery.com/jquery-migrate-3.4.1.js"></script>



<br />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateConfirmed" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="TenKhachHang" class="control-label">TÊN KHÁCH HÀNG</label>
                <input id="customer" asp-for="TenKhachHang" class="form-control" />
                <span asp-validation-for="TenKhachHang" class="text-danger"></span>
            </div>
            <br />


            <div class="form-group">
                <label asp-for="MaSP" class="control-label">TÊN SẢN PHẨM</label>
                <select id="sp" asp-for="MaSP" asp-items="@(new SelectList(ViewData["Products"] as List<ProductManagement.Models.Products>, "MaSP", "TenSP"))" class="form-control"></select>
                   
            </div>
            <br />
            <div>Giá: <span id="GiaCa" style="color:tomato"></span></div>
            <br />
            <div class="form-group">
                <label asp-for="SoLuongDH" class="control-label">SỐ LƯỢNG ĐẶT HÀNG</label>
                <input asp-for="SoLuongDH" class="form-control" />
                <span asp-validation-for="SoLuongDH" class="text-danger"></span>
            </div>
            <br />
            THÀNH TIỀN:
            <br />  
            <br />  
            <div class="form-group">
                <input type="submit" value="Tạo đơn hàng" class="btn btn-primary" />  |  <a asp-action="Index">Back to List</a>
        
            
        </form>
    </div>
</div>



@section Scripts {
@*     <script>
        $(document).ready(function () {
            $("#MaSP").change(function () {
                var maSP = $(this).val();
                $.ajax({
                    url: "/Order/GetGiaCa",
                    data: { maSP: maSP },
                    success: function (data) {
                        $("#GiaCa").val(data);
                    }
                });
            });
        });
    </script> *@
    <script>
        $("select")
            .on("change", function () {
                var str = "";
                $("select option:selected").each(function () {
                    str += $(this).text() + " ";
                    //xử lí code
                    $GetGiaCa(sp);

                });
                $("#GiaCa").text(str);
            })
            .trigger("change");
    </script>
}
